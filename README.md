# 运行

```powershell
python .\worker.py --config .\config.json --once --dry-run
```

```powershell
.\publish\QingyanMover.exe --config .\config.json --once --dry-run
```

```powershell
dotnet publish .\QingyanMover.csproj -c Release -o .\publish
```

请先编辑 `config.json` 和 `accounts.csv`。
